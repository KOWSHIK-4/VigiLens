{
  "version": 2,
  "installCommand": "npm ci && npx prisma migrate deploy",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/api/index"
    }
  ]
}
