{
  "version": 2,
  "installCommand": "npm ci && npx prisma migrate deploy && npx tsx prisma/seed.ts",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/api/index"
    }
  ]
}
