-- Superseded legacy migration applied directly to the development database.
-- Its effects are fully covered by the committed migrations that follow it,
-- so this is recorded as applied to keep history in sync without a reset.
SELECT 1;